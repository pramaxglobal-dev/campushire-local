# CampusHire Repository Cleanup Plan

**Generated**: 2026-07-03  
**Purpose**: Remove stale, duplicate, and misleading files to prepare repository for production development

---

## Executive Summary

This cleanup plan identifies files and directories that should be removed, archived, or reviewed before production development. The repository contains significant audit artifacts, duplicate documentation, and build artifacts that should not be in the main codebase.

**Total Files Identified for Action**: 25+ files/directories  
**Risk Level**: Low to Medium (no production code deletion proposed)

---

## Phase 1: Audit Artifacts (REMOVE)

### High Priority - Machine-Generated Audit Files

These are JSON files generated by previous audit scripts. They are large, not source code, and should be removed from version control.

| File/Path | Size | Action | Risk | Reason |
|-----------|------|--------|------|--------|
| `audit_api_client_calls.json` | 25KB | Remove | None | Generated audit artifact, not source |
| `audit_api_routes.json` | 87KB | Remove | None | Generated audit artifact, not source |
| `audit_api_routes_detailed.json` | 149KB | Remove | None | Generated audit artifact, not source |
| `audit_api_routes_enriched.json` | 99KB | Remove | None | Generated audit artifact, not source |
| `audit_controller_service_calls.json` | 15KB | Remove | None | Generated audit artifact, not source |
| `audit_mobile_pages.json` | 800B | Remove | None | Generated audit artifact, not source |
| `audit_model_usage_counts.json` | 28KB | Remove | None | Generated audit artifact, not source |
| `audit_prisma_models.json` | 215KB | Remove | None | Generated audit artifact, not source |
| `audit_schema_exports.json` | 5KB | Remove | None | Generated audit artifact, not source |
| `audit_service_models.json` | 5KB | Remove | None | Generated audit artifact, not source |
| `audit_web_page_usage.json` | 52KB | Remove | None | Generated audit artifact, not source |
| `audit_web_pages.json` | 35KB | Remove | None | Generated audit artifact, not source |

**Evidence**: All files are in root `campushire/` directory, contain JSON audit data, not referenced in any source code.

**Cleanup Command**:
```bash
cd campushire
rm audit_*.json
```

---

## Phase 2: Duplicate Documentation (REMOVE)

### Duplicate Documentation Files

| File/Path | Action | Risk | Reason |
|-----------|--------|------|--------|
| `docs/00_PROJECT_OVERVIEW - Copy.md` | Remove | None | Exact duplicate of docs/00_PROJECT_OVERVIEW.md |
| `docs/01_VISION_TO_CODE_MAPPING - Copy.md` | Remove | None | Exact duplicate of docs/01_VISION_TO_CODE_MAPPING.md |

**Evidence**: Files have "- Copy" suffix, identical content to non-copy versions.

**Cleanup Command**:
```bash
cd campushire/docs
rm "* - Copy.md"
```

---

## Phase 3: Build/Runtime Artifacts (REMOVE)

### Python Cache Files

| Directory/Path | Action | Risk | Reason |
|----------------|--------|------|--------|
| `apps/ai/app/__pycache__/` | Remove | None | Python bytecode cache, should be in .gitignore |
| `apps/ai/app/utils/__pycache__/` | Remove | None | Python bytecode cache |
| `apps/ai/app/services/__pycache__/` | Remove | None | Python bytecode cache |
| `apps/ai/app/routers/__pycache__/` | Remove | None | Python bytecode cache |
| `apps/ai/app/models/__pycache__/` | Remove | None | Python bytecode cache |

**Evidence**: Contains `.pyc` files, Python generates these automatically. `.gitignore` should be updated.

**Cleanup Command**:
```bash
cd campushire
find apps/ai -type d -name __pycache__ -exec rm -rf {} +
```

### TypeScript Build Artifacts

| File/Path | Action | Risk | Reason |
|-----------|--------|------|--------|
| `apps/web/tsconfig.tsbuildinfo` | Remove | None | TypeScript build cache, regenerated on build |

**Evidence**: Generated by `tsc --incremental`, not source code.

**Cleanup Command**:
```bash
cd campushire/apps/web
rm tsconfig.tsbuildinfo
```

---

## Phase 4: Old Audit Reports (ARCHIVE)

### Previous Audit Documentation

These are valuable for historical reference but should be moved to an archive folder, not kept in root.

| File/Path | Action | Risk | Reason |
|-----------|--------|------|--------|
| `CAMPUSHIRE_AUDIT.md` | Archive | Low | Previous comprehensive audit (853 lines) |
| `RUNTIME_AUDIT.md` | Archive | Low | Previous runtime audit findings |
| `DEMO_CREDENTIALS_SUMMARY.md` | Archive | Low | Demo credentials summary (superseded by docs/16_DEMO_LOGIN_CREDENTIALS.md) |
| `PRODUCTION_LAUNCH_PLAN.md` | Archive | Low | Previous launch planning document |
| `SECRETS_ROTATION_CHECKLIST.md` | Archive | Low | Previous security checklist |

**Recommendation**: Create `docs/archive/` directory and move these files there.

**Cleanup Command**:
```bash
cd campushire
mkdir -p docs/archive
mv CAMPUSHIRE_AUDIT.md docs/archive/
mv RUNTIME_AUDIT.md docs/archive/
mv DEMO_CREDENTIALS_SUMMARY.md docs/archive/
mv PRODUCTION_LAUNCH_PLAN.md docs/archive/
mv SECRETS_ROTATION_CHECKLIST.md docs/archive/
```

---

## Phase 5: .gitignore Updates (UPDATE)

### Missing .gitignore Patterns

The current `.gitignore` is missing patterns for Python and TypeScript build artifacts.

**Current .gitignore**: `campushire/.gitignore`

**Additions Needed**:
```gitignore
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
apps/ai/.env
apps/ai/.env.local

# TypeScript build artifacts
*.tsbuildinfo
dist/
```

**Evidence**: Found `__pycache__` directories and `tsconfig.tsbuildinfo` in repo.

---

## Phase 6: Demo/Development Scripts (REVIEW)

### Scripts Requiring Review

| File/Path | Action | Risk | Reason |
|-----------|--------|------|--------|
| `scripts/reset-demo-passwords.ts` | Review | Medium | Demo password reset script - ensure not used in production |
| `tools/generate_audit_docs.js` | Review | Low | Empty file (0 bytes) - either implement or remove |

**Evidence**: 
- `scripts/reset-demo-passwords.ts` contains logic to reset all demo passwords to `Test@123`
- `tools/generate_audit_docs.js` is 0 bytes, non-functional

**Recommendation**: 
- Keep `reset-demo-passwords.ts` but add safety check: only run if `DEMO_PASSWORD_RESET_ALLOWED=true`
- Remove `tools/generate_audit_docs.js` or implement if needed

---

## Phase 7: Documentation Review (KEEP BUT UPDATE)

### Documentation Files to Keep

These files are valuable but should be reviewed for accuracy:

| File/Path | Action | Reason |
|-----------|--------|--------|
| `docs/00_PROJECT_OVERVIEW.md` | Keep & Update | Good overview, but some info may be outdated |
| `docs/01_VISION_TO_CODE_MAPPING.md` | Keep & Update | Useful mapping document |
| `docs/02_SYSTEM_ARCHITECTURE.md` | Keep | Architecture documentation |
| `docs/03_CODEBASE_INVENTORY.md` | Keep & Update | May be outdated after cleanup |
| `docs/04_API_INVENTORY.md` | Keep & Update | Large file (93KB), may need regeneration |
| `docs/05_DATABASE_INVENTORY.md` | Keep & Update | Database schema documentation |
| `docs/06_UI_PAGE_INVENTORY.md` | Keep & Update | UI inventory may be outdated |
| `docs/07_AUTH_ROLE_PERMISSION_AUDIT.md` | Keep | Security documentation |
| `docs/08_DEPLOYMENT_AUDIT.md` | Keep & Update | Deployment documentation |
| `docs/09_TESTING_MASTER_PLAN.md` | Keep | Testing strategy |
| `docs/10_LIMITATIONS_AND_KNOWN_ISSUES.md` | Keep & Update | Important for context |
| `docs/11_DEVELOPMENT_RULES.md` | Keep | Development guidelines |
| `docs/12_MVP_SCOPE_LOCK.md` | Keep | Scope definition |
| `docs/13_FIX_PHASE_PLAN.md` | Archive | Previous fix plan, historical |
| `docs/14_CHANGELOG_AND_FIX_LOG.md` | Keep & Update | Change history |
| `docs/15_HANDOVER_FOR_NEXT_AI.md` | Archive | Handover document, historical |
| `docs/16_DEMO_LOGIN_CREDENTIALS.md` | Keep | Current demo credentials |

---

## Phase 8: Environment Files (SECURITY CHECK)

### Environment File Status

| File/Path | Status | Action | Risk |
|-----------|--------|--------|------|
| `.env` | Exists locally | Ensure not committed | High |
| `.env.example` | In repo | Keep, update placeholders | None |
| `apps/ai/.env` | Exists locally | Ensure not committed | High |
| `apps/ai/.env.example` | In repo | Keep, update placeholders | None |

**Evidence**: 
- `.gitignore` correctly lists `.env`, `.env.local`, `.env.*.local`, `apps/ai/.env`, `apps/ai/.env.local`
- `git ls-files .env` returns no tracked paths (good)
- However, `.env` file exists locally with real values

**Recommendation**: 
- Verify `.env` is not tracked: `git ls-files .env apps/ai/.env`
- Add pre-commit hook to prevent .env commits
- Update `.env.example` with stronger placeholder examples

---

## Cleanup Execution Order

### Step 1: Safe Removals (No Risk)
```bash
cd campushire
# Remove audit JSON files
rm audit_*.json

# Remove duplicate docs
cd docs
rm "* - Copy.md"
cd ..

# Remove Python cache
find apps/ai -type d -name __pycache__ -exec rm -rf {} +

# Remove TypeScript build artifacts
rm apps/web/tsconfig.tsbuildinfo
```

### Step 2: Archive Old Audits
```bash
cd campushire
mkdir -p docs/archive
mv CAMPUSHIRE_AUDIT.md docs/archive/
mv RUNTIME_AUDIT.md docs/archive/
mv DEMO_CREDENTIALS_SUMMARY.md docs/archive/
mv PRODUCTION_LAUNCH_PLAN.md docs/archive/
mv SECRETS_ROTATION_CHECKLIST.md docs/archive/
mv docs/13_FIX_PHASE_PLAN.md docs/archive/
mv docs/15_HANDOVER_FOR_NEXT_AI.md docs/archive/
```

### Step 3: Update .gitignore
```bash
cd campushire
# Add to .gitignore:
# __pycache__/
# *.py[cod]
# *.tsbuildinfo
```

### Step 4: Review Scripts
```bash
cd campushire
# Review tools/generate_audit_docs.js (0 bytes - remove or implement)
# Review scripts/reset-demo-passwords.ts (add safety check)
```

### Step 5: Verify Git Status
```bash
cd campushire
git status
# Ensure no .env files are staged
# Review all changes before commit
```

---

## Post-Cleanup Verification

### Files That Should Remain
- All source code in `apps/`, `packages/`, `prisma/`
- All active documentation in `docs/` (non-archived)
- Configuration files: `package.json`, `turbo.json`, `docker-compose.yml`
- `.env.example` files
- CI/CD config: `.github/workflows/ci.yml`

### Files That Should Be Gone
- All `audit_*.json` files
- All `__pycache__` directories
- All `*- Copy.md` duplicate files
- `tsconfig.tsbuildinfo` files
- Archived audit reports in `docs/archive/`

### Git Status Expected
```bash
# Modified: .gitignore
# Deleted: audit_*.json (12 files)
# Deleted: apps/ai/app/**/__pycache__ (directories)
# Deleted: apps/web/tsconfig.tsbuildinfo
# Deleted: docs/* - Copy.md (2 files)
# Renamed: CAMPUSHIRE_AUDIT.md -> docs/archive/CAMPUSHIRE_AUDIT.md
# Renamed: RUNTIME_AUDIT.md -> docs/archive/RUNTIME_AUDIT.md
# etc.
```

---

## Risk Assessment

### Low Risk Removals
- Audit JSON files (not referenced in code)
- Python cache files (regenerated automatically)
- TypeScript build artifacts (regenerated on build)
- Duplicate documentation files

### Medium Risk Actions
- Archiving old audit reports (keep for reference)
- Updating .gitignore (ensure correct patterns)
- Reviewing demo password script (ensure production safety)

### High Risk Actions (None Proposed)
- No source code deletion proposed
- No production configuration changes
- No database schema changes

---

## Approval Required

Before executing this cleanup plan:

- [ ] Review all files to be removed
- [ ] Confirm no audit JSON files are needed by current tooling
- [ ] Verify archived documents are not actively referenced
- [ ] Test that build still works after cleanup
- [ ] Confirm .gitignore updates are correct
- [ ] Get approval from tech lead/founder

---

## Estimated Time

- **Execution**: 15 minutes
- **Verification**: 10 minutes
- **Total**: 25 minutes

---

## Next Steps After Cleanup

1. Regenerate any needed audit documentation from current code
2. Update documentation inventories to reflect current state
3. Add pre-commit hooks to prevent future artifact commits
4. Update CI/CD to clean build artifacts
5. Document cleanup in changelog

---

**Cleanup Plan Status**: Ready for Review and Approval  
**Prepared By**: Code Audit  
**Date**: 2026-07-03
